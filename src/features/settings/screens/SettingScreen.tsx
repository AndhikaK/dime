import { Text } from "react-native";

import { SafeAreaView } from "react-native-safe-area-context";

const SettingScreen = () => {
  return (
    <SafeAreaView>
      <Text>Settings lagi</Text>
    </SafeAreaView>
  );
};

export default SettingScreen;
