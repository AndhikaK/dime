import { SafeAreaView } from "react-native-safe-area-context";

import { Typography } from "@/components/Typography";

const HomeTab = () => {
  return (
    <SafeAreaView>
      <Typography>HomeTab</Typography>
    </SafeAreaView>
  );
};

export default HomeTab;
